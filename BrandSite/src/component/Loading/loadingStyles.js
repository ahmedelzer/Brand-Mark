const loadingStyles = {
  container: "flex space-x-2 justify-center items-center h-screen",
  srOnly: "sr-only",
  bounce1:
    "h-8 w-8 !bg-accent !text-accent rounded-full animate-bounce [animation-delay:-0.3s]",
  bounce2:
    "h-8 w-8 bg-accent rounded-full animate-bounce [animation-delay:-0.15s]",
  bounce3: "h-8 w-8 bg-accent rounded-full animate-bounce",
};

export default loadingStyles;
